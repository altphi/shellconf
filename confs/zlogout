# Check and reset .zcompdump if too large
MAX_SIZE=1048576
if [[ -f ~/.zcompdump && $(stat -f %z ~/.zcompdump 2>/dev/null || stat -c %s ~/.zcompdump 2>/dev/null) -gt $MAX_SIZE ]]; then
    rm -f ~/.zcompdump
    echo "Removed large .zcompdump file"
fi
clear